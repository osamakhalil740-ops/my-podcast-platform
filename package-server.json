{
  "name": "podcast-server",
  "version": "1.0.0",
  "description": "JSON Server for Podcast Platform",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --routes routes.json",
    "dev": "json-server --watch db.json --port 3001 --routes routes.json --middlewares cors.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
